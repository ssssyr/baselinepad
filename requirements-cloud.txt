# PAD 环境（云端服务器）依赖清单
# - 默认使用清华 PyPI 镜像，加上 PyTorch CU121 国内镜像；若需 CPU 版本，可删除“+cu121”并去掉 extra-index。
# - 建议使用 Python 3.9/3.10/3.11/3.12（Torch cu121 仅支持到 3.12）。

-i https://pypi.tuna.tsinghua.edu.cn/simple
--extra-index-url https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/pytorch/wheels/cu121
--extra-index-url https://download.pytorch.org/whl/cu121

# gym==0.21.0 sdist fails to build with newer setuptools; pin an older version
setuptools==65.5.1

# Core DL stack
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
timm==0.9.12
diffusers==0.25.0
accelerate==0.24.1
transformers==4.36.2

# General utilities
numpy==1.24.4
scipy==1.10.1
PyYAML==6.0.1
tqdm==4.66.1
matplotlib==3.8.2
pillow==10.1.0
opencv-python==4.8.1.78
decord==0.6.0
mediapy==1.1.8
wandb==0.15.12

# RL / simulation stack
gym==0.21.0
mujoco-py==2.1.2.14
cython==0.29.36
pygame==2.5.2
glfw==2.6.2
robosuite==1.4.1

# Dev / optional tools
memory-profiler==0.61.0
pytest==7.4.3

# Install bundled metaworld package
-e ./metaworld
